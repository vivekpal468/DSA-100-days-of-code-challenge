#include <iostream>
using namespace std;
int mini(int arr[], int i, int n)
{
    if(i==n-1) return arr[i];
    return min(arr[i], mini(arr, i+1, n));
}
int main() {
    int arr[] = {8,6,7,2,6,4};
    cout<<mini(arr,0, 5);
    return 0;
}
