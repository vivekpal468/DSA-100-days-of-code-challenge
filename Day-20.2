#include <iostream>
using namespace std;
int maxi(int arr[], int i, int n)
{
    if(i==n-1) return arr[i];
    return max(arr[i], maxi(arr, i+1, n));
}
int main() {
    int arr[] = {8,6,7,2,6,4};
    cout<<maxi(arr,0, 5);
    return 0;
}
