#include <iostream>
using namespace std;
void reverse(string &str, int ind)
{
    if(ind==-1) return ;
   str[ind] = 'A'+str[ind]-'a';
    reverse(str, ind-1);
}
int main() {
    string str="VIVEKPAL";
    reverse(str, str.length()-1);
    cout<<str;
    return 0;
}
