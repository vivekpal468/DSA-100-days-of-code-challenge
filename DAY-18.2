// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int GCD(int a, int b)
{
    if(b==0) return a;
    return GCD(b, a%b);
}
int main() {
    int n, m;
    cin>>n;
    cin>>m;
    cout<<GCD(n, m);
    return 0;
}
